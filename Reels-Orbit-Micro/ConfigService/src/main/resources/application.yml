spring:
  profiles:
    active: native
  application:
    name: configServer
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations

server:
  port: 8083